#Strings are immutable which means if you apply any function on string you can not change them

name = "hamza"
capName = name.capitalize()
print("function make it capitaliz:",capName) #this function make a new variable didnt change the old one
print(name) #it is same as before


a = "Hamza" #String in double quotes
b = 'Hamza' #String in single quotes
c = '''Hamza''' #String in Triple single quotes
d = """Hamza""" #String in Triple double quotes